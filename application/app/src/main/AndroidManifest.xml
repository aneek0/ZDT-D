<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Needed to request installing APK updates downloaded by the app (online update). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Android 13+ runtime permission (requested only when user enables notifications). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Needed to list all installed apps (incl. system) for the app picker on Android 11+. -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />


    <application
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AW22xxxConfig">

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".WorldMapActivity"
            android:exported="false" />

        <activity
            android:name=".LocalWebPanelActivity"
            android:exported="false" />

        <activity
            android:name=".T2sPanelActivity"
            android:exported="false" />


        <activity
            android:name=".ProxyInfoProbeDetailsActivity"
            android:exported="false" />

        <service
            android:name=".tile.ZdtdTileService"
            android:label="@string/qs_tile_label"
            android:icon="@drawable/ic_qs_tile"
            android:exported="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Receives daemon state broadcasts from the root daemon via `am broadcast`. -->
        <receiver
            android:name=".DaemonStateReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.zdtd.service.ACTION_DAEMON_STATE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".ProxyInfoProbeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.zdtd.service.ACTION_PROXYINFO_PROBE_DETECTED" />
            </intent-filter>
        </receiver>



        <receiver
            android:name=".widgets.ZdtdMiniDashboardWidgetProvider"
            android:label="@string/widget_mini_dashboard_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_mini_dashboard_info" />
        </receiver>

        <service
            android:name=".diagnostics.nfqws.NfqwsTesterOverlayService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
